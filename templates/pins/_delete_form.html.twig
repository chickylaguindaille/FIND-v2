<form method="post" action="{{ path('app_pins_delete', {'id': pin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pin.id) }}">
    <button class="btn">Delete</button>
</form>
