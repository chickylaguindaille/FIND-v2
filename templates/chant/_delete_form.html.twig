<form method="post" action="{{ path('app_chant_delete', {'id': chant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chant.id) }}">
    <button class="btn">Delete</button>
</form>
