{% extends 'base.html.twig' %}

{% block title %}Decorum{% endblock %}

{% block body %}
    <h1>Decorum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decorum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ decorum.name }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ decorum.source }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ decorum.description }}</td>
            </tr>
            <tr>
                <th>Idcorporation</th>
                <td>{{ decorum.idcorporation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_decorum_index') }}">back to list</a>

    <a href="{{ path('app_decorum_edit', {'id': decorum.id}) }}">edit</a>

    {{ include('decorum/_delete_form.html.twig') }}
{% endblock %}
